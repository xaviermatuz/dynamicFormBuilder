/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./index.html", // Vite entry point
        "./src/**/*.{js,jsx}", // All JS and JSX files in src
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};
